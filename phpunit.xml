<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         failOnWarning="true"
         failOnRisky="true">
    <testsuites>
        <testsuite name="commons">
            <directory>tests</directory>
            <!-- Fixture .php files carry #[Group] tokens for the ScopeCoverage checker to scan;
                 they are data, not runnable tests. -->
            <exclude>tests/Fixtures</exclude>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <!-- A fixed key so the Testbench app (Livewire's render pipeline encrypts) boots; the
             value is base64 of a 32-byte constant, deterministic across runs. -->
        <env name="APP_KEY" value="base64:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY="/>
    </php>
</phpunit>
