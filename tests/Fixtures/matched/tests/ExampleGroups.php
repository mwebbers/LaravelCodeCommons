<?php

// Fixture, not a runnable test (tests/Fixtures is excluded from the suite). The coverage
// checker scans .php files under a tests dir for #[Group(...)] / @group tokens; this file
// carries exactly the two feature IDs that the matched SCOPE.md defines.

// #[Group('F-001')]
// #[Group('F-002')]
