<?php

// Fixture, not a runnable test. References F-001 (defined in the mismatched SCOPE.md) and
// F-999 (NOT defined there — a `stale` entry). F-003 in SCOPE.md has no group here (`missing`).

// #[Group('F-001')]
// #[Group('F-999')]
